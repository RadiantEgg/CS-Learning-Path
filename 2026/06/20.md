# 2026-06-20

##  新学习
- 内存池
- 结构体位域：减少内存占用
- 联合体类型：不同类型成员共用同一存储空间，但是不同时使用
- 内存块的赋值与拷贝：比循环效率高
```c
void *memset(void *ptr, int value, size_t num);

// 用于数组清零: int a[10]; memset(a, 0, sizeof(a));

memcpy(void *ptr, const void *src, size_t num);     // 按照字节拷贝
// 用于数组拷贝: int a[10], b[10]; memcpy(a, b, sizeof(b));


```

##  待解决
- 